# Server configuration
PORT=3000
NODE_ENV=development

# Upload settings
MAX_FILE_SIZE_MB=500
UPLOAD_DIR=./uploads
OUTPUT_DIR=./output

# Processing options
# Default quality: low | medium | high
DEFAULT_QUALITY=high

# GPU settings (auto-detected if not set)
# FORCE_ENCODER=libx264   # Force specific encoder for testing

# Frontend (Vite dev server)
VITE_API_BASE_URL=http://localhost:3000

# Python path (auto-detected if not set)
# PYTHON_BIN=python3
